public class Trenary_Operators {
    public static void main(String args[]) {
        int februaryDays = 29;
        // boolean result;
        String result;
        // var = (exp) ? (exp1) : (exp2)
        // If exp is true, exp1 is assigned to the var else exp2 is assigned to the var.
        result = (februaryDays == 28) ? "Not a leap year" : "Leap year";
        System.out.println(result);

    }
}
